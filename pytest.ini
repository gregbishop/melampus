[pytest]
# Running pytest from the repo root would otherwise collect the archived _old/
# tree, whose stale melampus package shadows the real one on sys.path.
testpaths = service/tests
norecursedirs = _old .venv fixtures fixtures_full .melampus_cache
